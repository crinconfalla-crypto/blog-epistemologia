document.addEventListener("DOMContentLoaded",()=>{const menu=document.querySelector(".menu-btn"),links=document.querySelector(".nav-links");menu?.addEventListener("click",()=>links.classList.toggle("open"));document.querySelectorAll(".nav-links a").forEach(a=>a.addEventListener("click",()=>links.classList.remove("open")));document.querySelectorAll(".options button").forEach(btn=>btn.addEventListener("click",()=>{const f=document.getElementById("quiz-feedback");if(btn.dataset.answer==="right"){f.textContent="✓ Respuesta correcta: una actitud epistemológica crítica revisa la evidencia y también las condiciones del procedimiento, en lugar de proteger la hipótesis a cualquier costo."}else{f.textContent="La epistemología invita a revisar críticamente la evidencia, el diseño y las explicaciones posibles; no a eliminar datos incómodos."}}));const speak=document.getElementById("speakBtn");speak?.addEventListener("click",()=>{if(!("speechSynthesis"in window)){alert("Tu navegador no dispone de lectura de texto.");return}window.speechSynthesis.cancel();const text=document.querySelector("#pregunta").innerText;const u=new SpeechSynthesisUtterance(text);u.lang="es-ES";u.rate=.95;speechSynthesis.speak(u)});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add("visible")}),{threshold:.12});document.querySelectorAll(".card,.thinker,.media-card,.stat").forEach(e=>io.observe(e));});
